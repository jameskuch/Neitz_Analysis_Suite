__pycache__/
*.pyc
*.pyo

# Jupyter checkpoints
.ipynb_checkpoints/

# OS files
Thumbs.db
.DS_Store

# Editor/IDE
.vscode/
.idea/

# Claude Code local config (machine-specific) — keep out of the repo
.claude/

# Generated figures / analysis outputs (these live in ~/Documents/ephysdataio,
# not in the repo)
*.png
*.pdf
*.svg

# Python packaging / build artifacts
*.egg-info/
build/
dist/
.pytest_cache/

# Scratch notebooks (kept locally, not in repo)
STA_test.ipynb
testingbed.ipynb

# Generated analysis CSVs from examples run in the repo root (real outputs go to the store)
*_metrics.csv
barak-*.csv

# Native-window launcher bundles — these bake in machine-specific absolute paths, so keep them
# out of the repo; regenerate with scripts/build_macos_app.py / build_windows_app.py (build/ too).
NeitzAnalysisSuite.app/
NeitzAnalysisSuite.vbs

# …but DO track the app-icon source + rendered icons (they override the blanket *.svg / *.png above).
!assets/app_icon.svg
!assets/app_icon.icns
!assets/app_icon.ico
!assets/favicon.ico
!assets/icon-192.png
!assets/icon-512.png
